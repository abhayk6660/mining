# 🪓 Auto Mining AFK Bot

A Minecraft bot that automatically logs in, warps, and mines blocks around it using Mineflayer.

### ⚙️ Features
- Auto login & warp
- Auto mining (stone, ores, etc.)
- Auto reconnect on disconnect
- Works 24/7 on Render, Railway, etc.

### 🚀 Setup Instructions
1. Upload these files to your hosting platform.
2. Run `npm install`.
3. Start with `npm start` or `node index.js`.

### ⚠️ Notes
- Tested with Node.js 22+
- Minecraft version: 1.21.1
- Change `username`, `host`, and `/login` command as needed.
